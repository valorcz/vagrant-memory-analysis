version: '3'

services:
  memory-analysis:
    image: pv204_memory_analysis
    container_name: memory_analysis
    # platform: linux/amd64
    build: .
    volumes:
      - data:/opt/vagrant

volumes:
  data:
